name: authelia

x-daemonless:
  title: "Authelia"
  icon: ":simple-authelia:"
  category: "Infrastructure"
  description: "Authelia on FreeBSD."
  upstream_url: "https://github.com/authelia/authelia"
  web_url: "https://authelia.com/"
  freshports_url: "https://www.freshports.org/net-p2p/authelia/"
  user: "bsd"
  mlock: false
  community: "Discord:https://discord.gg/Kb9tkhecZT"
  # Host networking: Authelia's configuration.yml points its session store
  # at redis on 127.0.0.1, which is true on podman and AppJail alike.
  appjail:
    director:
      options:
        - alias:
        - ip4_inherit:
    depends_on:
      - name: authelia-redis
        image: ghcr.io/daemonless/redis:latest
        volumes:
          redis_data: /config

  docs:
    env:
      PUID: "User ID for the application process"
      PGID: "Group ID for the application process"
      TZ: "Timezone for the container"
      CONFIG_LOCATION: "Path to the Authelia configuration directory (configuration.yml, users database)"
      REDIS_DATA_LOCATION: "Path to store the redis session store"
    volumes:
      /config: "Configuration directory"
    ports:
      9091: "Web UI"

services:
  authelia-server:
    image: ghcr.io/daemonless/authelia-server:latest
    container_name: authelia-server
    restart: unless-stopped
    network_mode: host
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ:-UTC}
    volumes:
      - ${CONFIG_LOCATION}:/config
    ports:
      - 9091:9091
    depends_on:
      - redis

  redis:
    container_name: authelia_redis
    image: ghcr.io/daemonless/redis:latest
    restart: unless-stopped
    network_mode: host
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${REDIS_DATA_LOCATION}:/config
